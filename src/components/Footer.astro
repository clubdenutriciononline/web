---
import { socialLinks, legalLinks } from '../data/navigation';
import SocialIcon from './SocialIcon.astro';

const year = new Date().getFullYear();
---

<footer class="site-footer">
  <img
    src="/images/site/banner-fondo-trans.png"
    alt="banner-fondo-trans"
    width="988"
    height="169"
    class="footer-banner"
  />

  <div class="footer-black">
    <div class="footer-inner">
      <div class="footer-social">
        {socialLinks.map((s) => (
          <a href={s.href} aria-label={s.label} target="_blank" rel="noopener noreferrer">
            <SocialIcon name={s.icon} />
          </a>
        ))}
      </div>

      <nav class="footer-legal" aria-label="Enlaces legales">
        <ul>
          {legalLinks.map((l) => (
            <li><a href={l.href}>{l.label}</a></li>
          ))}
        </ul>
      </nav>

      <p class="footer-copy">Copyright © {year} Club de Nutrición</p>

      <button class="back-to-top" type="button" aria-label="Volver arriba">↑</button>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    text-align: center;
  }
  .footer-black {
    /* Confirmado por captura real: banda negra a ancho completo del
       viewport para redes/legales/copyright — la imagen decorativa
       "banner-fondo-trans" queda fuera, sobre fondo blanco. */
    background: var(--color-footer-bg);
    color: var(--color-footer-text);
  }
  .footer-inner {
    max-width: var(--container-width);
    margin-inline: auto;
    padding: 1.5rem var(--container-padding) 2rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
  }
  .footer-legal ul {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 1rem;
    list-style: none;
    margin: 0;
    padding: 0;
  }
  .footer-legal a {
    color: var(--color-footer-text);
  }
  .footer-social {
    display: flex;
    gap: 0.75rem;
    color: var(--color-footer-text);
  }
  .footer-banner {
    max-width: 100%;
    height: auto;
  }
</style>
